/**
 * X/Twitter card image — same card as the OG image, declared explicitly so
 * twitter:image is always present instead of relying on og:image fallback.
 */
export { default, alt, size, contentType } from "./opengraph-image";
